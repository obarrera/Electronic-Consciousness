# Direct dependencies only — pip resolves the transitive ones (the previous
# file was a full `pip freeze` of an unrelated environment, including
# matplotlib, requests, rich, and PyInstaller leftovers the game never imports)
numpy==1.26.4
pygame==2.6.0
PyOpenGL==3.1.7
PyOpenGL-accelerate==3.1.7
tensorflow==2.16.2
